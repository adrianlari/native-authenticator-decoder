export enum TextTypes {
  DOT = "dot",
  ADDRESS = "address",
  BODY = "body",
  BLOCKHASH = "blockhash",
  EXTRAINFO = "extrainfo",
  HOST = "host",
  SIGNATURE = "signature",
  TTL = "ttl",
}

export default TextTypes;
